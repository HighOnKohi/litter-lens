import 'package:flutter/material.dart';

class GuideTab extends StatelessWidget {
  const GuideTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Guide Page', style: TextStyle(fontSize: 24)),
    );
  }
}
